# Port the HTTP server listens on (ALB/target group targets this).
PORT=8080

# Shared secret for service-to-service auth. MUST match RESOLVER_API_KEY set
# on the Unfaked web app. Generate with: openssl rand -hex 32
RESOLVER_API_KEY=
